#include "main.h"

void Function_Stop(RobotContext_t *ctx)
{
   printf("STOP\n");
    Motion_Stop();
    LED_Red_On();
    if(ctx->input.btn_checkpoint == 1)
    {
        LED_Red_Off();
        ctx->state.current = STATE_CHECK_POINT;
    }
}
