#include "main.h"


void Function_Initialize(RobotContext_t *ctx)
{
   // printf("Initialize\n");
    Motion_Stop();
    LED_Green_On();
    LED_Red_Off();
    ctx->state.current = STATE_LINE_FOLLOWING;
}
