#include "main.h"

void Function_PickBall(RobotContext_t *ctx)
{
   printf("Pick ball");
    Motion_Stop();

    if(ctx->time.t_pickup > pickup_timeout)
    {
        ctx->flag.pick_ball = 1;
        ctx->state.current = STATE_REVERSE;
    }
}
