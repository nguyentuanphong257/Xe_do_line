#include "Config/config.h"


void State_Checkpoint(RobotContext_t *ctx)
{
    if ((!ctx->flag.pick_ball == 1) || (!ctx->flag.turned == 1)) 
    {
      pickup_timeout = 0;
      ctx->state.current = LINE_FOLLOWING;
    } else if (!ctx->flag.drop_ball = 1) 
    {
      ctx
    }
}
