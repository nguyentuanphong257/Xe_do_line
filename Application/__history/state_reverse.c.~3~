#include "Config/config.h"


void State_Reverse(RobotContext_t *ctx)
{
    Motion_Backward();

    if()
    {
        Motion_Stop(50,50);

        ctx->last_state = ctx->state;
        ctx->state = STATE_TURN_RIGHT;
    }
}
