#include "gpio_input.h"

/*************************************************
                GPIO INIT
**************************************************/

void GPIO_Input_Init(void)
{
    set_tris_b(0xFF);

    // N?u có thêm Port A, C...
    // s? thêm ? dây
}

/*************************************************
                READ GPIO
**************************************************/

int1 GPIO_ReadPin(int pin)
{
    return input(pin);
}
